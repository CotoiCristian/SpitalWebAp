@model IEnumerable<SpitalFinal.Models.ProgramLucru>

@{
    ViewData["Title"] = "Index";
}
<head>
    <link rel="stylesheet" href="~/css/Index.css" asp-append-version="true" />
</head>
<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeZi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataLibera)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeZi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataLibera)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProgramLucruId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProgramLucruId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProgramLucruId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
